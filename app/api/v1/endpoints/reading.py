"""
Reading progress tracking endpoints.
"""

from fastapi import APIRouter

router = APIRouter()

# TODO: Implement reading progress endpoints
# - GET /reading/progress (get user's reading progress for all books)
# - GET /reading/progress/{ebook_id} (get reading progress for specific book)
# - PUT /reading/progress/{ebook_id} (update reading progress)
# - POST /reading/bookmarks/{ebook_id} (add bookmark)
# - GET /reading/bookmarks/{ebook_id} (get bookmarks for book)
# - DELETE /reading/bookmarks/{bookmark_id} (delete bookmark) 